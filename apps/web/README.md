# Web App Workspace Shim

This workspace is a thin shim so monorepo tools can target `apps/web`.

The existing Next.js app source remains at repository root intentionally,
to avoid changing Vercel behavior.
